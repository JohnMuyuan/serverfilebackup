JOB_NAME="mail-server"
REMOTE_HOST="mail.example.com"
REMOTE_USER="backup"
SSH_KEY="$HOME/.ssh/backup_ed25519"

SOURCE_PATHS=(
  "/var/vmail"
  "/etc/postfix"
  "/etc/dovecot"
)

EXCLUDE_PATTERNS=("*.lock" "*.tmp")
RETENTION_COUNT="90"
TRANSFER_MODE="mirror"
PARALLEL_TRANSFERS="4"
